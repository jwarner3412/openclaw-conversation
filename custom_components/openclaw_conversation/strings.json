{
  "config": {
    "step": {
      "user": {
        "title": "Connect to OpenClaw",
        "description": "Enter your OpenClaw Gateway URL and API token.",
        "data": {
          "name": "Name",
          "base_url": "OpenClaw Gateway URL",
          "api_key": "API Token",
          "model": "Model",
          "timeout": "Timeout (seconds)"
        }
      }
    },
    "error": {
      "cannot_connect": "Unable to connect to OpenClaw Gateway. Check the URL.",
      "invalid_auth": "Invalid API token.",
      "endpoint_disabled": "Chat Completions endpoint is not enabled on the Gateway. Add gateway.http.endpoints.chatCompletions.enabled: true to your OpenClaw config."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "OpenClaw Options",
        "data": {
          "persistent_conversation_id": "Persistent Conversation ID (leave empty for per-turn sessions)",
          "max_conversation_messages": "Max conversation messages (default 50)",
          "agent_id": "Agent ID (default: main)",
          "timeout": "Timeout in seconds (default 30)",
          "inactivity_reset_minutes": "Reset history after this many minutes of inactivity (0 disables)",
          "risky_confirmation_enabled": "Require confirmation for risky actions"
        }
      }
    }
  }
}